"""Pin Police scorers.

- ``geometry``: pure-Python checks (re-exported from the package). Reused by the
  export guardrail.
- ``weave_scorers``: ``weave.op`` scorer functions + the LLM-judge class, for
  use inside ``weave.Evaluation``.
"""
